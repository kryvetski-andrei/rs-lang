import { pageClassName } from '../../config';
import { sprintPageId } from './config';

import './index.scss';

export const gamesPageMarkup = `
  <section id="${sprintPageId}" class="${pageClassName}">Sprint</section>
`;