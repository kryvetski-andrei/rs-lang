import { pageClassName } from '../config';
import { registrationPageId } from './config';

import './index.scss';

export const registrationPageMarkup = `
  <section id="${registrationPageId}" class="${pageClassName}"></section>
`;
